# 虛擬留言模板池消耗注意事項

> ⚠️ `CRON_VIRTUAL_COMMENTS` 模板池約 80 條，每條用過即排除（全站不重複）。
> 依目前 Cron 每天約 30% 機率留言的頻率，池子預估 **2-4 週內用完**。

## 用完後的行為
- 模板池耗盡後，Cron 不會再為一般用戶產生模板留言
- 有 `ai_user_summary` 的用戶（蘿生門、pop）仍會透過 Gemini LLM 生成留言

## 後續處理建議
1. **擴充模板池**：在 `lib/commentFallbackPool.ts` 的 `CRON_VIRTUAL_COMMENTS` 中新增更多留言
2. **全面改用 LLM**：為所有重要用戶設定 `ai_user_summary`
3. **回收機制**：DB 中 7 天以上的虛擬留言會被清除，但目前不會自動將模板放回可用池
